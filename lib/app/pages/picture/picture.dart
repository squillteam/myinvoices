import 'package:flutter/material.dart';

/// Picture Page
class PicturePage extends StatelessWidget {

  static String routeName = "/picture";

  @override 
  Widget build(BuildContext context) {
    return Container(
      child: Text("Picture Page")
    );
  }
}